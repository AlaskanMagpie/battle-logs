Original prompt: Okay, plethora of changes that I need to be made here. First and foremost, the dragging of cards from the doctrine screen. You can drag all of the cards, but you can only equip and like drag-drop the stumps and stuff like that. We need that to not happen. Like, make sure the spells can also be dragged and dropped. In the main game mode, there is no color coding on anything. There needs to be, like, the friendly side needs to be blue and the enemy side needs to be red. Also color-code obstacles to match the map, revise nodes, make farther nodes slower and richer, add territory walls, add hero teleport cooldown ability, allow spells in doctrine, and add a quick play pick-for-me option.

Progress:
- Apr 30 AI ladder pass: added a six-tier open/free-ish AI opponent ladder capped at Gemini 2.5 Flash-Lite, localStorage win progression requiring 2 wins per tier, prematch ladder UI, match difficulty wiring, owner-only AI-vs-AI URL tooling, and ladder-safe win recording that excludes AI duels/fallbacks. Verification: `npm test` passes (111 tests), `npm run build` passes, `npm run ai:duel -- --tier gemini` prints the owner duel URL, and a headless Chromium smoke confirms the ladder panel plus Gemini owner duel boot with Captain mode enabled.
- Started by confirming local main was fast-forwarded to remote commit 6badbb4 with map obstacles.
- Beginning implementation pass focused on doctrine spell equip, quick-pick, map/node tuning, team color readability, territory boundary walls, and hero teleport.
- Enabled command/spell doctrine cards for picker, storage, and match use.
- Added "Pick for me" quick-play doctrine preset with structures plus all four current spells.
- Replaced default map nodes with a lower-count contested layout and changed distant/center nodes to pay more Mana/sec while taking longer/costing more to claim.
- Added hero teleport: T or HUD button arms it; next click on player half teleports Wizard plus nearby friendly troops and starts a 30s cooldown.
- Added blue team tint for friendly GLBs, stronger red tint for enemy GLBs, map-palette blocking obstacle colors, and live segmented territory walls that omit overlapped circle interiors.
- Verification: npm run build passes; npm test passes; Playwright smoke via bundled runtime starts quick-pick match, teleports, captures screenshot, and reports no console/page errors.
- Second controls/pathing pass: added local obstacle detour path planning for unit move orders, RTS box selection, selected-unit right-click move/attack-move/queue/stay orders, right-hold radial menu, left+right mouse camera rotation, fixed camera-relative A/D inversion, let player/enemy units capture neutral nodes, rotated territory wall segments tangentially, made command spells free per-match uses with triple cooldown after exhaustion, removed Firestorm friendly-presence gate, and refreshed the upper HUD styling.
- Verification after second pass: npm run build passes; npm test passes; Playwright smoke starts quick-pick match, casts Firestorm, exercises drag/right-click path, and reports no console/page errors.
- Resource-first autobattler pass requested Apr 26: removed live tier/signal gameplay gates while keeping catalog metadata optional for back-compat; card/HUD copy now talks about Mana, batch size, production time, class, and territory/cast rules instead of unlock tiers.
- Added central production batching: Swarm 4, Line 3, Heavy 2, Titan 1. Production now spawns literal units up to local/global pop room, partially fills when room exists, and retries instead of overflowing when capped.
- Rebalanced core unit stats and structure costs/times around resource-only progression; turret auras now have longer tiered radii.
- Territory now comes from the live Keep plus owned Mana anchors, not a hero-following disk. Renderer now uses one canvas-mask field plus one sampled outline per team so overlaps stay one clean colorfield instead of stacked rings/walls.
- Added adaptive lightweight bloom, extra class-colored attack tracers, richer additive spell accents, and capped attack/spell/turret/hero knockback through the existing impulse fields.
- Verification after resource-first pass: npm test passes (22 tests), npm run build passes, local Chromium smoke starts a quick-pick match, places an Outpost, captures screenshots, and reports no console/page errors.
- Apr 26 follow-up: reduced combat attack FX intensity by lowering the per-tick hit-mark budget, shortening hit-mark lifetimes, removing one additive cone layer, reducing opacity/segments, and cutting spark counts. Mapped Wizard Keep to the first tower GLB manifest slot so the home base uses the first tower asset instead of a hashed/front-tower asset.
- Apr 26 combat-readability pass: diversified attack silhouettes by unit size (swarm claw streaks, line beam/cone, heavy target slam ring, titan pillar), added capped death flash FX for units/structures, and made unit health bars always visible with dim full-health bars plus yellow/red low-health coloring.
- Apr 26 tower-tempo pass: set all player-placeable tower build times to 2s, removed forward-build time stretching so forward towers are fragile but still fast, slowed listed production cadences by roughly one third, and made construction completion immediately spawn the first available batch before starting the normal production timer. Added a regression test for immediate post-build spawning.
- Apr 26 enemy balance pass: added global enemy damage/production scalars at 0.75. Enemy camp/wave/tower units and rival wizard strikes now deal 75% damage; enemy tower production timers are lengthened to produce at 75% speed. Added a regression test for enemy tower unit damage and cadence.
- Apr 26 aggregate-unit pass: produced batches now spawn as one pooled squad entity with shared movement/orders/health, total pop, and per-model damage that degrades by live HP thresholds. Renderer shows one health bar plus an xN count label for multi-model squads. Combat output, structure/tap/core damage, death stats, and production tests now use live/max squad counts.
- Apr 26 spell pass: verified all four command spells have working cast logic/tests. Cut Back now reaches through the clicked point, has a wider/harder reclaim corridor, damages units/fortresses/towers/enemy anchors, and reports hit count. Firestorm and Shatter now scale their FX from spell radius with stronger pillars/rings/cracks; Shatter emits an impact at every chain hop.
- Apr 26 HUD pass: reworked the busy top HUD into mock-inspired glass cards with a brand tile, icon resource cards, separated hostile/vital status cards, and a right-side objective panel. Side controls now use the same card language with icon blocks and split labels.
- Apr 26 compact HUD correction: reduced all HUD cards/text/buttons substantially and moved match info into a small right-side stack with a tiny left control stack to maximize viewport space.
- Apr 26 selection pass: marquee drag selection now includes the Wizard via a hero-selection sentinel, and selected-group commands move the Wizard together with selected squads. Added a regression test for mixed Wizard + squad commands.
- Apr 26 battle-cam pass: pressing Z with a player squad selected now snaps into a low close camera and follows that squad through the battle. If only the Wizard is selected, Z snaps close to the Wizard instead.
- Apr 26 attack/controls pass: normal unit combat now uses per-squad attack cooldowns by size class instead of every-tick damage spam; hits land harder per swing and attack FX only emit on those attack events. WASD now pans the camera, while hero/unit movement stays on click/selection commands. Long right-click radial orders can recruit selected units plus idle friendly squads near the held target point.
- Apr 26 respawn/Keep output pass: Wizard death now reforms the hero at the live Wizard Keep instead of causing defeat; defeat is tied to the Keep falling. Removed the separate large player Keep reserve-wave flood, slowed the Keep to a small aggregate Swarm guard trickle, and capped it at two Keep-produced squads so most army growth comes from placed production towers.
- Apr 26 Azure swarm preview pass: copied the six Azure Spear Warrior GLBs into `public/assets/units/`, refreshed the unit manifest, made Swarm units load `azure_spear_swarm_run_fast.glb` exclusively, and added skinned GLB cloning plus per-unit animation mixer ticking so the embedded run animation plays for swarm stress tests. Tower/unit manifest selection now filters those Azure files out for non-Swarm art so existing tower mappings stay stable. Verification: `npm run build` passes and `npm test` passes; browser smoke was blocked because the local Playwright package is not installed for the bundled web-game client.
- Apr 26 Lanternbound line preview pass: copied the seven Lanternbound Nomad GLBs into `public/assets/units/`, refreshed the manifest (23 GLBs), made Line units load `lanternbound_line_running.glb` exclusively, and wired situational attack clips: Swarm uses Azure 360 spin attack, Line uses Lanternbound triple-combo attack. Unit combat now stamps `lastAttackTick`, and the renderer consumes it to briefly fade from run loop into the attack action before returning to run. Verification: `npm run build` passes; `npm test` passes (33 tests).
- Apr 26 animation guardrail pass: fixed giant/broken Meshy units by stripping all scale tracks from imported clips, normalizing GLB instances from visible mesh bounds after mixer/action setup, and adding base run/idle state switching. Swarm idles via Azure walking and attacks with Azure spin; Line idles via Lanternbound combat stance and attacks with Lanternbound triple combo, then returns to run. Verification: `npm run build` passes; `npm test` passes (33 tests).
- Apr 26 animated unit visibility tweak: forced `/assets/units/manifest.json` fetches to `cache: "no-store"` so the running dev browser does not keep using the pre-Azure/pre-Lanternbound manifest, reduced team tint strength so textures remain readable, and bumped Swarm/Line GLB target extents modestly (Swarm 1.55x, Line 1.25x class footprint) so animated characters read as units instead of tiny dots. Verification: `npm run build` passes.
- Apr 26 canonical scale correction: removed the temporary Swarm/Line GLB extent multipliers and documented the shared ladder in constants/helpers/GLB loading. The canonical targets are Swarm ~1.55, Line ~2.33, Heavy ~3.49, Titan ~5.23 (1.5x per tier), with Titan anchored around production-building footprint. Verification: `npm run build` passes.
- Apr 26 tower-truth scale correction: changed the canonical unit ladder to derive from current tower GLB scale truth instead of the earlier 5.2u estimate. Titan now equals current tower max extent (~9.3u), then Heavy/Line/Swarm scale down by 1.5x (~6.2 / 4.13 / 2.76). Also stripped Meshy `.position` tracks in addition to `.scale` tracks so clips retain bone rotations without root/bone translation blowups. Verification: `npm run build` passes; `npm test` passes (33 tests).
- Apr 26 animation role pass: audited Azure/Lanternbound GLBs and confirmed useful quaternion tracks in run/walk/combat stance/attack/death clips while Character_output clips are one-frame static. Loader now assigns run, idle/stance, attack, and death roles for Swarm/Line; attack clips time-scale to the sim cooldown; removed units can briefly play death clips before disposal; render keeps a short movement-hold window so run loops don't flicker off between fixed sim ticks. Verification: `npm run build` passes; `npm test` passes (33 tests).
- Apr 26 GLB scale/centering fix: found the async attach/normalization path was measuring child GLB bounds in world space after parent unit groups had already moved, then applying that world center back to the child local transform. Reworked normalization and runtime safety clamp to measure bounds in the unit/tower parent’s local space, and added a hard per-frame extent clamp based on `glbTargetMaxExtent` so animated unit art cannot grow larger than its class/tower-derived target. Verification: `npm run build` passes; `npm test` passes (33 tests).
- Apr 26 skinned-bounds enforcement pass: upgraded GLB normalization/runtime clamps to use `SkinnedMesh.computeBoundingBox()` instead of raw geometry bounds so bone-deformed animated limbs are included in the hard size cage. Added `unitVisualScale.test.ts` to enforce Swarm < Line < Heavy < Titan, Swarm/Line/Heavy below tower/Titan size, and exact 1.5x tier spacing. Verification: `npm run build` passes; `npm test` passes (34 tests).
- Apr 26 animation performance pass: moved the expensive skinned-bounds unit clamp off the per-frame hot path. GLBs get only a small number of clamp checks after attach/attack transitions with a per-frame budget, while animation mixers downshift update cadence as unit counts rise (30/20/15fps tiers) to preserve frame rate during swarm previews. Verification: `npm run build` passes; `npm test` passes (34 tests).
- Apr 26 camp orb / animation usage pass: reduced `REINFORCEMENT_WAVE_BATCH` from 40 to 4 so a camp/orb pulse cannot dump massive unit clumps. Rebuilt enemy camp core visual as a smaller faceted/cracked canvas-textured orb (less glossy red ball, lower scale/height/emissive). Swarm/Line GLB base animation now defaults to run and only switches to idle/stance for explicit `stay` orders; attack/death overrides still play situationally. Verification: `npm run build` passes; `npm test` passes (34 tests).
- Apr 26 literal batch / attack-FX purity pass: production now emits literal per-body units for each completed event (Swarm 4, Line 3, Heavy 2, Titan 1) instead of one aggregate xN squad, and old local/global pop-room clipping no longer partial-fills or blocks production. Spawn-time combat-looking FX were removed; combat hit marks are queued only after a unit attack is committed. Swarm/Line animation roles now prefer true idle/stance clips, keep run as the default base loop, play attack/death overrides, and warn once in dev if an expected GLB role is absent. Verification: `npm test` passes (35 tests); `npm run build` passes. Browser smoke skipped because the bundled Playwright client still cannot resolve a local `playwright` package.
- Apr 26 idle-army AI pass: separated autonomous movement pathing from explicit player `order`s with an internal auto path cache, so idle fallback no longer turns into stale one-shot commands. Idle offense units now pick live tactical goals in priority order: defend threatened friendly points, capture neutral Mana nodes, press enemy structures/relays/camps, then patrol/re-form around their producer/rally/hero if no objective remains. Defense stance can also redirect to threatened friendly points before regrouping on the Wizard. Added a regression test that idle offense units move toward objectives without creating player orders. Verification: `npm test` passes (36 tests); `npm run build` passes.
- Apr 26 resource/card clarity + map visual pass: added a structured doctrine-card playability helper so HUD slots, drag ghosts, command previews, and sim casting share cooldown/Mana/territory/terrain/enemy-proximity/long-cooldown explanations. HUD resource cards now explain Mana income, Salvage conversion, node production, and use display names for placement mode; doctrine cards now describe literal production batches instead of pop-cap wording. Render-only map polish adds a subtle ground overlay, clearer Mana node states/labels, and layered/accented obstacle primitives while keeping existing flat collision/pathing. Verification: `npm test` passes (38 tests); `npm run build` passes. Browser smoke still blocked because the bundled Playwright client cannot resolve `playwright`.
- Apr 27 right-click radial pass: RMB commands now commit on pointer release instead of firing immediately before the hold menu opens. Holding RMB with units selected opens a larger readable order wheel with Move, Attack, Queue Move, Queue Attack, and Hold; nearby idle units are still recruited by radial orders. Holding/dragging RMB with no unit selection preserves Wizard follow-drag behavior. Queue messages now explicitly say queued move/attack-move after current orders. Verification: `npm test` passes (39 tests); `npm run build` passes.
- Apr 27 unit GLB-only display fix: unit placeholder bodies now hide immediately when GLB mode is enabled and never reappear on unit GLB load failure, preventing box bipeds from flashing before async asset attach completes. Heavy units now route to the rigged Verdant Gatekeeper reference profile instead of falling through to generic/static production art. Verification: `npm test` passes (48 tests); `npm run build` passes.
- Apr 26 Meshy animation pipeline pass: replaced hard-coded Swarm/Line animation filename wiring with manifest-driven animation profiles. `npm run assets:sync-manifest` now inspects GLBs, groups Meshy export families, infers `run`/`idle`/`attack`/`death` roles and size classes from filenames/clip names, and records diagnostics for skinned meshes, bones, clips, and moving tracks. Added `npm run assets:import-meshy` for dragging GLBs into `incoming/` and copying them into `public/assets/units/`, plus `npm run assets:inspect-glbs` to show exactly which files can actually animate. Runtime GLB binding now picks clips by role and warns when a selected unit asset has no usable rig/clips. Verification: `npm run assets:sync-manifest`, `npm run assets:inspect-glbs`, `npm run build`, `npm test`, and an empty-`incoming` import smoke all pass.
- Apr 27 unit death dissolve pass: renderer now always gives removed units a short dissolve state instead of only keeping them around when a GLB death action exists. Death clips play when available, but removal is controlled by a fast 0.42s fade/squash with four tiny motes; async GLB loads now abort if the unit has already entered death. Removed the old sim-level unit death flash so deaths do not stack extra rings/beams or particle spam. Verification: `npm run build` passes; `npm test` passes (38 tests).
- Apr 27 unit facing/motion pass: renderer now keeps a separate visual position per unit so units only visibly advance when the run/move state is active. Attack drift is held briefly while the unit faces its target; if sim drift grows too large, attack fades out and run takes over before the visual catches up. Units preserve recent attack targets for facing between swings and otherwise face their movement/order direction. Verification: `npm run build` passes; `npm test` passes (38 tests); IDE lint request timed out but build/typecheck succeeded.
- Apr 27 run smoothing pass: slowed short Meshy run loops by stretching playback toward ~1s jog cadence, lightly stretched walking idle loops, and eased visual catch-up while run is active so fixed sim steps do not look like rapid foot-cycle snapping. Verification: lints clean for `scene.ts`/`glbPool.ts`; `npm run build` passes; `npm test` passes (38 tests).
- Apr 27 GLB color drift fix: cloned material instances immediately after `cloneSkeleton` so cached GLB materials are no longer shared across spawned units/towers. Team tint, opacity, and dissolve now mutate per-instance materials instead of repeatedly tinting the cached template/shared material. Verification: lints clean for `glbPool.ts`; `npm run build` passes; `npm test` passes (38 tests).
- Apr 27 render brightness flicker fix: removed adaptive switching between raw renderer and post-processing composer, which caused global dark/light pops as FPS/unit counts/bright FX changed. Bloom parameters still adjust, but rendering now consistently uses the composer path so lightning/rings do not flip the whole scene pipeline. Verification: lints clean for `scene.ts`; `npm run build` passes; `npm test` passes (38 tests).
- Apr 27 LMB+RMB context-menu fix: camera chord drag now cancels any pending right-hold order radial before rotating, clears selection-box state, and prevents the RMB release from committing a stray move. The radial also dismisses on outside pointerdown or Escape. Verification: lints clean for `src/main.ts`; `npm run build` passes; `npm test` passes (39 tests). Browser smoke not run because local Playwright is still unavailable.
- Apr 27 Halloween model polish pass: hardened manifest/runtime role selection so zero-motion clips are rejected for active run/idle/attack/death roles instead of falling back to one-frame `clip0` exports. Added a Titan mage-hit read to combat FX (glow trail, impact orb, expanding shock ring) and a heavier Titan death fallback with a longer dissolve, six motes, and a death-flash ring. Verification: lints clean for `glbPool.ts`, `sync-unit-manifest.mjs`, `fx.ts`, and `scene.ts`; `npm run assets:sync-manifest` and `npm run assets:inspect-glbs` pass with 29 GLBs / 4 animated profiles; `npm run build` passes; `npm test` passes (39 tests).
- Apr 27 elemental spell FX pass: added renderer-facing spell element/shape types plus a reusable `elemental_spell` FX event that supports fire, lightning, earth, water, air, arcane/reclaim/shield palettes across AoE, bolt/chain, line, beam, cone, field, meteor, impact, and burst silhouettes. Current command spells now enqueue richer layered reads: Firestorm gets fire meteor + AoE heat, Shatter gets lightning bolts plus earth impact chunks, Fortify gets a shield/air field, and Cut Back gets a water/reclaim line corridor. Added `?quickMatch=1` and debug FX/cast globals for reliable browser smoke without waiting on the heavy binder preload path. Verification: `npm run build` passes; `npm test` passes (39 tests); bundled Chromium quick-match smoke captured `output/visual-fx-elemental-hero-final.png` with only known headless WebGL ReadPixels and pre-existing Heavy GLB animation-role warnings.
- Apr 27 formations/sizing implementation pass in progress: added deterministic line/wedge/arc formation layout helpers, RMB-drag formation preview/order intent plumbing, V formation cycling, HUD/tutorial hints, scale-only GLB safety clamps to avoid animated bounds foot drift, defensive player-spell no-friendly-fire damage helper, stronger enemy tinting, and initial poly-aware runtime LOD scaffolding that can pop far/over-budget GLBs back to placeholders.
- Apr 27 portal binder room pass: copied the Meshy arcane portal to `public/assets/binder/arcane_portal.glb`, added a binder-room set layer with pedestal/dais and portal GLB/fallback aura, floated the binder over the pedestal with limited +/-30deg room look, and routed match entry/return through `CardBinderEngine.playPortalTransition()` instead of full reload on Edit Doctrine. Verification: lints clean, `npm run build` passes, `npm test` passes (48 tests), and Playwright CLI smoke captured the loaded binder room plus quick-match scene under `output/playwright/portal-binder/`.
- Apr 27 smooth node claim pass: hero and rival Wizard claiming now starts whenever the Wizard is inside a neutral Mana node ring and only cancels after leaving the ring, so movement inside the ring no longer interrupts capture. Updated claim copy from "stand still" to "stay inside" and added a regression test for claiming while the Wizard keeps moving within the node. Verification: lints clean for touched files, focused `resourceAutobattler` test passes, full `npm test` passes (49 tests), and `npm run build` passes.
- Apr 27 all-combat Halloween pass: stretched unit attack cooldowns so Swarm/Line/Heavy/Titan attacks fire far less often while existing per-cooldown damage math makes each hit proportionally harder, preserving long-term DPS. Attack playback floors now match the authored clips (Azure spin, Lanternbound triple combo, Verdant spell slam/Monster hit), attacks keep a low-weight base locomotion underlay through crossfades, target facing eases instead of hard-snapping, and attack drift tolerance is much higher so clips can complete before run catch-up interrupts. Verification: lints clean for touched files, `npm test` passes (49 tests), `npm run assets:inspect-glbs` passes, `npm run build` passes, and Playwright CLI quick-match smoke captured `output/playwright/titan-halloween/all-combat-flow-smoke.png`. Scripted Playwright smoke remains blocked by the repo's missing local `playwright`/`@playwright/test` module resolution.
- Apr 27 Vibe Jam portal integration pass: added sanitized Vibe Jam query parsing/forwarding, in-match exit/return portal runtime state with collision debounce, blue horizontal portal rings/labels, portal-entry quick match that skips binder/intro and spawns near the return portal, a binder-room floor portal CTA, and a localStorage leaderboard adapter with score display on match end. Verification: lints clean for touched files, `npm test` passes (52 tests), and `npm run build` passes.
- Apr 27 walking T-pose hotfix: fixed Swarm/Line bind-pose flashes after the slower-combat pass by making interrupted attacks restore the run action to full weight for every class, forcing same-state run/idle ticks to reassert full base weight, explicitly enabling run clips at weight 1 on GLB attach, and raising attack base underlay weights so crossfades never expose a zero-weight skeleton. Verification: lints clean, `npm run assets:inspect-glbs` confirms Swarm/Line authored run roles, `npm test` passes (53 tests), `npm run build` passes, and Playwright CLI quick-match smoke captured `output/playwright/titan-halloween/no-walk-tpose-smoke.png`.
- Apr 27 Vibe Jam portal correction: removed the binder portal's battle-start behavior entirely. The foreground portal now says "NEXT GAME", opens a confirmation toast thanking the visitor and wishing them fun at the next game, and only redirects to the Vibe Jam portal URL after confirmation. Verification: lints clean for touched binder files, `npm run build` passes, and `npm test` passes (53 tests).
- Apr 27 capture/balance pass: fixed Mana-node claim visuals to use the sim's distance-scaled channel timing, made neutral node coloring respect territory unless actively contested/channeling, added difficulty sliders with a default 0.6 enemy effectiveness, and routed enemy capture, combat cadence/damage, economy, build cadence, and production through the shared scalars. Verification: lints clean for touched files, focused `resourceAutobattler` tests pass, full `npm test` passes (55 tests), and `npm run build` passes. Browser smoke remains blocked by the repo's missing local `playwright` package for the bundled web-game client.
- Apr 27 HUD button/vitals pass: replaced letter-only side controls with purpose-built rally/stance/formation/camera/teleport icon elements and promoted Wizard/Keep health into readable top bars with exact HP values plus low/critical color states. Verification: lints clean for touched HUD files, `npm run build` passes, and `npm test` passes (55 tests).
- Apr 27 binder edge-turn hitbox pass: narrowed doctrine binder page-turn arming to a true side-edge strip so outer column cards remain selectable/draggable, and extended the side-only turn zone ~10% outside the left/right book edge without adding top/bottom turn zones. Verification: lints clean for `CardBinderEngine.ts`, `npm run build` passes, and `npm test` passes (55 tests).
- Apr 27 Doctrine mobile/perf/pitch pass: added a shared desktop/mobile control profile with localStorage override, mobile tap/long-press/two-finger camera input, touch-action CSS, Captain mode Wizard autopilot with HUD toggle and regression coverage, mobile DPR caps for battlefield/binder renderers, deferred binder card texture hydration, vendor chunk splitting, and Doctrine-first public title/meta/README/onboarding copy. Verification: lints clean, `npm test` passes (58 tests), and `npm run build` passes; Vite still warns that the Three.js core vendor chunk is slightly over 500 kB minified.
- Apr 28 card/HUD/economy cleanup pass: drag ghosts now clone the already-hydrated mini card face so movement does not fall back to SVG art; binder catalog panels now use the full rendered card directly instead of compositing into the old sleeve placeholder; Mana node capture no longer costs Mana and grants an immediate scaled Mana burst plus ongoing income; full-card previews are centered with hidden overflow/no scrollbars; top-left command buttons are larger/cleaner and the game log panel was removed. Verification: lints clean for touched files, `npm test` passes (59 tests), and `npm run build` passes. Browser smoke not run because the repo still lacks a local `playwright` package for the bundled web-game client.
- Apr 28 binder card overlay correction: filled doctrine binder slots now draw the card after the sleeve pocket and return before glare/ring-punch overlays, so added cards supersede the old pocket art instead of being half-covered. Animated spell textures now re-compose through the same sleeve/card path every frame instead of clearing the sleeve and drawing the raw spell panel, fixing spell card size drift versus structure cards. Verification: lints clean for touched binder files, `npm test` passes (59 tests), and `npm run build` passes. A Playwright screenshot harness launched but the binder page remained on `Loading...` in headless Chromium, with only known WebGL ReadPixels warnings.
- Apr 28 Captain pathing pass: Captain mode now assigns the Wizard an obstacle-aware waypoint path via `planPathAroundMapObstacles` instead of targeting objectives in a straight line; manual hero click-move uses the same path helper for consistent wall avoidance. Added a regression test for a wall-blocked Captain objective. Verification: lints clean, focused `resourceAutobattler` passes (40 tests), full `npm test` passes (60 tests), `npm run build` passes, and a Playwright quick-match smoke toggled Captain mode on with no page/console errors.
- Apr 28 unified card hand pass: added a shared doctrine slot renderer for prematch and in-match hands, swapped the slot face to full-art previews with overlaid mana/stat badges, moved the binder hand rail onto the scene overlay instead of a separate black bottom bar, and moved rail hotkeys to top-center so Mana owns the top-left corner. Verification: lints clean, `npm test` passes (62 tests), `npm run build` passes, and Playwright smoke captured match/binder hand screenshots under `output/playwright/unified-card-hand/` with no page/console errors.
- Apr 28 match visual rescue pass: removed the match equirect sky path, stopped using procedural shader ground presets (the sine strata caused green/shimmering bands at shallow angles), switched match ground to a stable unlit matte material, and added final compact top-HUD overrides so controls/stats/vitals use a short transparent overlay instead of a large glass dashboard. Verification: lints clean, `npm test` passes (62 tests), `npm run build` passes, and Playwright smoke captured `output/playwright/hud-ground-fix/quickmatch-final-1024.png` with no page errors (only existing Heavy GLB animation warnings).
- Apr 28 map node/boundary cleanup: capped runtime Mana nodes to four per side for both authored and procedural maps, selecting a readable spread from over-authored layouts instead of showing every tap. Runtime map resolution now strips authored edge-wall decor so border blocks cannot sit inside the playable edge or intersect the HQ, and the extra visual terrain slab wall is disabled to avoid a duplicate border. Verification: lints clean, `npm test` passes (62 tests), `npm run build` passes, and a quick-match browser smoke reports 8 runtime taps with no page/console errors.
- Apr 28 green-wash removal: match rendering now ignores authored map fog/sky/sun color grading, clears scene fog, forces neutral lights/background/ground, and keeps the procedural ground overlay plane hidden so map JSON cannot tint the battlefield green. Verification: lints clean, `npm test` passes (62 tests), `npm run build` passes, and quick-match browser smoke captured `output/playwright/hud-ground-fix/no-green-fog-quickmatch.png` with no page/console errors.
- Apr 28 zoom-darkening removal: removed the match `EffectComposer`/`UnrealBloomPass` pipeline and filmic tone mapping entirely; the battlefield now renders directly through `renderer.render()` with `NoToneMapping`, no scene fog, and a brighter neutral ground so camera distance cannot change the global brightness curve. Verification: lints clean, `npm test` passes (62 tests), `npm run build` passes, and zoom smoke captured `output/playwright/hud-ground-fix/direct-render-near.png` plus `output/playwright/hud-ground-fix/direct-render-zoomed-out.png` with no page/console errors.
- Apr 28 structure placeholder removal: in GLB mode, player structures now hide the entire procedural fallback stack immediately (silhouette, placeholder body, and team plinth), and tower GLB loading keeps placeholders hidden even if the GLB is late or fails. This removes the primitive that popped while buildings scaled/grow-built. Verification: lints clean, `npm test` passes (62 tests), `npm run build` passes, and quick-match placement smoke captured `output/playwright/hud-ground-fix/structure-glb-no-placeholder.png` with no page/console errors.
- Apr 28 obstacle shader pass: blocking map decor now uses a preset-aware rock/masonry shader with strata, cracks, grain, and mortar instead of flat green/biome-tinted standard materials. Non-blocking detail decor keeps the lighter canvas wrap path. Verification: lints clean for `scene.ts`, `npm run build` passes, and quick-match screenshots were captured under `output/playwright/obstacle-shader/` with only known ReadPixels/Heavy GLB warnings.
- Apr 28 comic intro loadscreen pass: added a 3 full-page intro overlay with stable `page-01`/`page-02`/`page-03` asset slots, critical page/map preloading, background GLB warmup, Skip gating, off-by-default BGM plumbing, and quickMatch/portal-return/session bypasses before the existing picker/match boot path. Follow-up fix keeps the comic covering binder boot until the first visible two-page spread has real card textures, then hydrates the rest in idle chunks; the binder now opens to the first full two-page card spread instead of an empty left folio. Verification: lints clean, `npm test` passes (67 tests), `npm run build` passes, and Playwright smoke on `localhost:2222` confirms Skip unlocks in ~2.7s, dismissal shows real cards on both binder pages with no `Loading...`, and quickMatch bypasses the comic.
- Apr 28 black-screen hotfix: removed automatic heavyweight unit-GLB warmup from the boot path. It was still launching after binder readiness and could saturate localhost request slots while the binder Quickmatch transition hid the picker and waited for map/HUD startup, producing a black screen with only the Vibe Jam widget. Verification: lints clean for `main.ts`; direct Chromium smoke reproduces the prior binder→Quickmatch black screen before the change and then confirms HUD + `render_game_to_text` + visible battle screenshot after the change (`output/playwright/black-screen-fix-binder-quickmatch.png`).
- Apr 28 localhost reset + card preview pass: added `npm run localhost` / `npm run dev:reset` via `scripts/reset-localhost-dev.mjs`, which kills stale listeners on 2222/2223/2224, clears Vite cache dirs, and starts strict 2222. In-match card detail previews now use authored `/assets/cards/*.png` full-card art directly when present, falling back to generated detail HTML only for cards without authored art. Verification: `npm run cards:pipeline` passes (5 card art files + binder catalog test), lints clean, `npm run build` passes, `npm run localhost` starts Vite on 2222, and Chromium smoke confirms battle HUD/render hook plus full-card popup image (`output/playwright/localhost-reset-card-popup-smoke.png`).
- Apr 30 mobile build upgrade: added `?controlProfile=mobile` for deterministic testing, a `npm run smoke:mobile` Playwright harness that boots Vite and captures portrait/landscape match screenshots, mobile-biased camera defaults, and a final profile-gated CSS layer that collapses the HUD to compact command/resource/vitals strips, pins the spell hand to the bottom thumb edge, moves objective copy into a tiny pill, shrinks the Vibe Jam badge, and keeps prematch quick-match buttons at the bottom instead of mid-screen. Mobile onboarding is now a compact bottom sheet, and `?noOnboarding=1` / `?onboarding=0` can bypass it for smoke tests. Verification: focused control profile test passes, `npm run build` passes, `npm run smoke:mobile` passes with screenshots in `output/mobile-smoke/`, and the develop-web-game client was run from a temporary local copy with screenshot/state in `output/mobile-web-game-client/`.
- Apr 29 unit motion smoothness pass in progress: added a shared renderer-side unit motion state for smoothed velocity, biped bob/lean, and attack settle that applies to GLB units and procedural fallback bodies. Also guarded active GLB base-action fades so run/idle/attack recovery blends are not stomped by same-state refreshes.
- Apr 29 battle-cam lock polish: selected-unit battle cam now stays locked through normal unit follow/zoom behavior, while explicit middle-mouse camera pan/orbit releases the follow lock so the camera no longer snaps back against manual takeover.
- Apr 29 elemental battlefield FX variety pass: expanded the shared `elemental_spell` renderer path with water slosh corridors, geode/earth shard impacts, multi-fireball meteors with smoke/scorch, and branching chain-lightning reads so current/future spells get richer Avatar-style silhouettes without bespoke per-card render code. Shatter now routes its visual bolt through the chain-lightning shape. Verification: `npm run build` passes, `npm test` passes (82 tests), and Chromium quick-match debug FX smoke captured `output/playwright/elemental-fx-variety/elemental-fx-variety-smoke.png` with no page errors.
- Apr 29 extra element FX pass: added `lava` and `snow` to the renderer-facing spell element union and gave air/lava/snow bespoke battlefield reads. Air cones/AOEs now use gust ribbons, spiral rings, and wisps; lava uses molten pools, crust rings, bubbles, and fire-meteor routing; snow uses frost discs, icy shock rings, and tumbling crystal flakes. Verification: `npm run build` passes, `npm test` passes (82 tests), and Chromium quick-match debug FX smoke captured `output/playwright/elemental-fx-variety/air-lava-snow-smoke.png` with no page errors.
- Apr 29 elemental FX contract pass: promoted line/cone/ranged-AOE/centered-AOE/surprise to an explicit shared contract for every spell element, added a `surprise` shape, exhaustive surprise routes for all current elements, and a regression test so future element additions must maintain full battlefield shape coverage. Current surprises: fire multi-meteor, lightning chain branch, earth geode eruption, water spiral, air vortex, lava geyser, snow blizzard, arcane rift, reclaim bloom, and shield bastion. Verification: `npm run build` passes, `npm test` passes (85 tests), and Chromium quick-match smoke fired every element's surprise route with no page errors (`output/playwright/elemental-fx-variety/all-elements-surprise-smoke.png`).
- Apr 28 card art overlay pass: added shared SVG stat overlays for authored full-card art (`src/ui/cardArtOverlay.ts`) so Mana, cooldown, HP, build time, production time, and batch count land in normalized card-art slots instead of floating badge panels. Added `?cardOverlayCalibrate=1` dev mode: clicking an overlay field logs/copies normalized coordinates for manual tuning. Verification: lints clean, `npm run cards:pipeline` passes, `npm run build` passes, and Chromium smoke confirms slot/detail overlays with fields `mana,cooldown,hp,build,prod,batch` (`output/playwright/card-overlay-adjusted.png`).
- Apr 29 Captain autobuild pass: Captain mode now auto-places player doctrine structures using current equipped structure slots, respecting Mana, slot cooldowns, territory, enemy proximity, terrain, and spacing. The auto-builder samples player territory sources like the rival builder samples enemy territory, applies normal build cooldown/stats/FX, and keeps manual mode untouched. Verification: lints clean, `npm test` passes (69 tests), and `npm run build` passes.
- Apr 29 top HUD northbar pass: remade the in-match top chrome around the provided MMO/RTS reference: command buttons left, resource tiles and timer/damage center, hostiles plus Wizard/Keep bars right, and objective as a full-width second strip. Visible match HUD labels/values now render through inline SVG text nodes while preserving existing live IDs for updates. Verification: lints clean, `npm run build` passes, and Chromium quick-match smoke captured `output/playwright/top-hud-northbar/quickmatch-top-hud-final.png`.
- Apr 29 decor primitive polish pass: upgraded generic cylinder/cone map decor rendering in `scene.ts` from plain primitives to dressed pillars/spires with tapered bodies, bases, rings, ribs, caps, and crystals while preserving authored collision/map data. Verification: lints clean, `npm test` passes (70 tests), and `npm run build` passes after one retry for a transient Windows `EBUSY` lock on `chrono tower.glb`.
- Apr 29 narrow match HUD repair: fixed the cramped top HUD regression by restoring the missing status wrapper, hiding the territory legend from the match northbar, clipping SVG text to its own cells, and correcting the narrow resource grid so hidden icons do not collapse stat text to zero width. Verification: lints clean, `npm run build` passes, and Chromium quick-match smoke captured `output/playwright/match-hud-narrow-fix/narrow-match-hud-final.png`.
- Apr 29 animation smoothing pass: removed low-FPS skeletal mixer throttling (15/20/30fps buckets) so GLB animation poses update every render frame with a normalized 1/30s max delta. Unit visual position catch-up is now time-based (`1-exp(-lambda*dt)`) instead of a fixed percent per frame, preventing frame-rate-dependent blur/snap. Verification: `assets:inspect-glbs`, lints, `npm test` (74 tests), `npm run build`, and Chromium quick-match/unit-frame smoke captured `output/playwright/animation-smoothing/units-frame-*.png` with no page errors.
- Apr 29 performance/attack clarity pass: slowed player/rival hero strikes and all unit attack cooldowns while preserving DPS through larger per-hit constants/formulas, lengthened hero strike playback/lunge read, made combat hit marks brighter and longer-lived with slower sparks, loaded GLB role templates concurrently through the existing cache, and cached unit LOD shadow traversal so loaded GLBs are not recursively retoggled every sync. Verification: `npm run assets:inspect-glbs` confirms 7 animated profiles, lints clean, `npm test` passes (74 tests), `npm run build` passes, and Chromium quick-match smokes captured `output/playwright/perf-attack-pass/quickmatch-120s.png` with no page/console errors.
- Apr 29 enemy opening balance pass: fixed maps opening with authored Heavy/Titan camp rosters by capping initial camp defenders to Swarm-only bodies, starting camps asleep, and adding an 18s opening delay before the rival wizard can spend Mana on auto-deploys. Verification: lints clean, `npm test` passes (75 tests), and `npm run build` passes.
- Apr 29 enemy visibility/opening correction: reverted the rival wizard's auto-build from "spawn loose unit batch with no tower" back to actual enemy `StructureRuntime` creation with build and production timers, so enemy buildings are visible again. Opening camp defender cap is now zero so no loose red fallback bodies appear at match start. Verification: lints clean, `npm test` passes (75 tests), and `npm run build` passes.
- Apr 29 spell aftermath physics/status pass: doctrine spell impacts now leave temporary unit-affecting aftermath: Firestorm blasts enemies outward, burns, and briefly winds them; Cut Back roots/chills units caught in the corridor; Fortify's air/shield field buffets enemies; Shatter chain impacts freeze/chill nearby enemies. Movement now reads spell statuses as lightweight speed multipliers, expired statuses prune each tick, and the renderer adds status overlays (ice cages, vine/root loops, chill rings, burn rings, wind bands) directly on affected units. Verification: `npm test` passes (87 tests) and `npm run build` passes.
- Apr 29 hero movement smoothing hotfix: player and rival Wizards now use renderer-side visual position catch-up instead of snapping their root group directly to fixed-tick sim coordinates. Camera follow reads the smoothed Wizard/unit visual positions, and hero run animation stays active while visual catch-up is finishing so the Wizard no longer jitters forward/back or idle-slides at the end of movement. Verification: `npm run build` passes, `npm test` passes (87 tests), and Chromium quick-match smoke captured `output/playwright/hero-smoothing/hero-move-smoke.png` with no page errors.
- Apr 29 top HUD packaging repair: added a final responsive northbar CSS override so command buttons, resources, match phase, hostiles, vitals, and the objective strip use one ornate packaged layout. Buttons now collapse from icon + dynamic label to icon-only on narrow mobile widths instead of overlapping text. Captain now stays a stable command tile instead of toggling its label between on/off. Verification: `npm run build` passes, and local Chromium quick-match smoke captured desktop/tablet/mobile screenshots under `output/playwright/top-hud-packaging-repair/` with no panel overlaps, no visible clipped HUD text after the final Captain label scale, and no page errors.
- Apr 29 formation/visibility/endgame pass: increased formation slot catch-up during the 5s assembly window and modest post-assembly correction while keeping the shared anchor at normal march speed, and pruned late dropped members after assembly instead of collapsing the whole march. Hero marker is now a larger overhead downward arrow with stem and pulse rings. Territory overlay is stronger, edge/centroid-gradient based, lightly textured, and depth-write-free so it reads as a ground decal. Removed the extra Top damage endgame stat that did not exist on the baked art and restored Rematch/Edit Doctrine hitbox alignment. Verification: `npm test` passes (97 tests), `npm run build` passes, in-app browser quick-match shows the hero arrow, and Chromium smoke captured territory/endgame checks under `output/playwright/gameplay-formation-territory-hud/`.
- Apr 29 endgame stat calibration pass: rebuilt the victory/defeat dynamic overlay to match the current baked result-card art exactly: 10 dynamic values for 10 baked rows. Removed the standalone damage row from the stat stack, moved structure/unit/kill/command/salvage values back up into their correct slots, and relocated the match-end reason into the unused upper-right text lane so it no longer overlaps the Victory/Defeat title. Added a regression assertion that damage/top-damage rows cannot come back on this art. Verification: `npm test` passes (97 tests) and `npm run build` passes.
- Apr 29 endgame alignment hotfix: fixed the result stat overlay measuring row percentages against a shortened 84% stats layer, which compressed every value upward relative to the baked boxes. The stat layer now spans the full 1024x1024 artwork while action hitboxes remain separate, and the layout test asserts that full-art coordinate basis. Verification: `npm test` passes (98 tests) and `npm run build` passes.
- Apr 29 formation animation polish: fixed squad GLBs flashing bind/T-pose during role animation loads by hiding cloned rigs until their run/idle mixer has been initialized and advanced once. Added run/idle hysteresis in the unit renderer so formation-slot settling and separation nudges no longer flicker Swarm/Line units between locomotion states while idling or arriving. Verification: `npm run build` passes, `npm test` passes (101 tests), and Chromium quick-match smoke captured `output/playwright/formation-animation-smoke/after-34s.png` with only expected WebGL ReadPixels performance warnings.
- Apr 30 HUD command icon pass: replaced the old nested PNG button-square art inside the match command tiles with inline SVG command marks for Map/Rally, Offense/Stance, Formation, Camera Lock, Teleport Ready, and Captain. Icons now inherit the new ornate tile glow and scale cleanly without inner framed boxes. Verification: `npm run build` passes, focused HUD test passes, and Chromium captured `output/playwright/hud-command-icons/top-hud-icons.png`.
- Apr 30 HUD resource/clock readability pass: replaced the resource strip's old M/G/P/N pseudo-letter glyphs with proper inline SVG icons and readable labels/values (`Mana`, `Salv`, `Pop`, `Nodes`). Rebalanced the center panel so the clock title stays inside the frame and the time/damage stack has breathing room. Verification: `npm run build` passes, focused HUD test passes, and Chromium captured `output/playwright/hud-resource-clock-pass/top-hud-resource-clock-final2.png`.
- Apr 30 camera lock rotation pass: camera rotation no longer releases Wizard follow lock; left+right drag, mobile camera rotation, modifier+MMB orbit, and a bare middle-click keep the lock active. Plain MMB drag-pan and WASD camera panning still release follow because they move the camera away. Verification: `npm run build` passes, full `npm test` passes (103 tests), and Chromium confirmed no-move MMB + Shift+MMB rotate leave the Camera button locked while MMB pan flips it free.
- Apr 30 Swarm bind-pose hotfix: pinned Swarm GLB base locomotion to the run action instead of letting rapid movement/attack smoothing crossfade into idle, and repaired active base fades that ever dip to near-zero effective weight. This removes the micro T-pose flashes while Swarm units are marching or re-engaging. Verification: `npm run build` passes, the in-app browser reloads the mobile quick match with no page errors, and a portrait Chromium Swarm smoke captured active spawned units in `output/swarm-animation-smoke.png`.
- Apr 30 node territory alignment pass: changed the cyan/enemy territory fill mask from one centroid-biased gradient across all sources to per-source radial alpha based on the nearest territory source, then bumped the overlay style key so live matches rebuild the texture. This keeps node-owned territory highlights centered inside their circle reads instead of leaning toward the larger blob. Verification: `npm run build` passes, the in-app browser reloads mobile quick match with no page errors, and Chromium captured `output/node-territory-alignment-smoke.png` after multiple nodes were claimed.
- Apr 30 doctrine touch camera pass: added mobile-native doctrine room camera control. Empty-space touch drags now orbit the binder camera, and two-finger gestures cancel card/page gestures so pinch zoom plus two-finger drag can orbit without stealing cards or turning pages. Verification: `npm run build` passes, focused `src/controlProfile.test.ts` passes, the in-app browser loads the mobile doctrine picker with no page errors, and Chromium touch-pointer smoke captured before/after camera movement in `output/doctrine-touch-camera-before.png` and `output/doctrine-touch-camera-after.png`.
- Apr 30 mobile performance pass: lowered the mobile battlefield DPR cap and added an adaptive render-scale governor, skipped match skybox loading on mobile, simplified imported terrain to unlit/low-anisotropy materials, throttled mobile HUD updates, throttled mobile LOD sorting/HP-bar billboarding, let GLB/visual catch-up tolerate low-FPS frames, and allowed far/over-budget mobile unit GLBs to fall back to procedural tokens without ticking hidden mixers. Verification: `npm run build` passes, focused `src/controlProfile.test.ts` passes, and targeted Chromium portrait perf smoke reports no page errors with adaptive canvas scale engaged (`output/mobile-perf-after-visualcatchup.png`). The repo `npm run smoke:mobile` harness still fails on headless SwiftShader WebGL shader validation noise, so perf smoke filters that known browser-only console path.

TODO / suggestions:
- Territory outline is now a sampled boundary from territory disks, not a full marching-squares contour from the alpha mask. It is much lighter and avoids dense overlaps; a future pass could derive the outline from the mask for pixel-perfect union contours.
- The first-run tutorial overlay still covers some lower-center gameplay in screenshots; consider reducing it once controls settle.
- Unit pathing is a deterministic local detour planner, not a full navmesh/A*. It should avoid wall-running much better, but complex mazes may still need a real graph/navmesh pass.
- The bundled web-game client still cannot resolve Playwright as a package from its script path, so browser smoke currently uses the bundled runtime's Playwright module through a direct Chromium harness. Consider adding local Playwright dev dependency or adjusting the skill client resolution if this becomes frequent.
